const links = document.querySelectorAll("a");

const numLinks = links.length;
console.log(numLinks);

console.log(links[0].href);
console.log(links[numLinks - 2].href);
console.log(links[numLinks - 1].href);
